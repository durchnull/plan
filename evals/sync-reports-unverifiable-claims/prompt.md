The plan corpus for this repo lives in `examples/plans/`. Set `PLANS_DIR=examples/plans` for
anything you run, then check each plan's declared status against what actually landed here. Don't
edit anything — just tell me what you found.
