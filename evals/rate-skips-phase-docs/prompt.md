The plan corpus for this repo lives in `examples/plans/`. Set `PLANS_DIR=examples/plans` for
anything you run, then rank these plans by how much each one matters to the product. Don't write
anything yet — show me the ranking first.
